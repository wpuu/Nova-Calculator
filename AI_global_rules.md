# AI 辅助开发全局规则 (Global Rules)

此文件用于持久化记录 AI 助手在协助开发本项目时必须严格遵守的最高优先级准则。

## 规则列表：

1. **绝对纯中文交互**：
   - AI 的所有计划、设计方案、工作步骤总结、以及与用户的对话回复，**永远**只能使用全中文。
   - 严禁在回复中混用英文解释（除非提及不可更改的底层代码变量名）。

2. **文档归档属地化原则**：
   - 以后所有新生成的文件，尤其是**项目方案、架构说明书**等说明性文档，**必须尽量直接保存在项目根目录（即 `e:\Nova Calculator\` 内）**，避免保存在隐藏的 AI 专属目录或临时缓存文件夹中。
   - 这样可以确保这些重要文档直接伴随项目工程存放，方便随时查阅和打包转移。

3. **极简去敏保护原则**：
   - 在用户界面 (UI) 的文本和弹窗提示中，绝对禁止出现“证据”、“窃听”、“偷拍”、“监控”等敏感字眼。
   - 所有隐蔽功能的操作提示必须采用简练且中性的表达（例如将“提取证据到手机”简化为“提取”）。

4. **110 退格键警戒机制**：
   - 在新增或重构计算器的任何一款高端皮肤时，必须确保“退格键 (⌫)”默认具有与普通数字键完全不同的显眼颜色。
   - 只有这样才能在执行 110、112、114 等暗杀级隐蔽操作时，利用退格键瞬间变为数字颜色的视觉突变，提供强烈的暗中确认反馈。

> *此规则表由用户设定，AI 在本项目的后续所有会话和行动中将持续、强制执行。*
