# 自动连点辅助功能分析与实现计划

## 1. 可行性分析 (Feasibility Analysis)

针对您提出的需求，在 Android 平台中实现方案如下：

1. **悬浮在最上层的圆圈 (Floating Circle)**
   * **技术方案**：使用 `WindowManager` 添加 `TYPE_APPLICATION_OVERLAY` 类型的窗口。
   * **可行性**：完全可行。该应用已经在 `AndroidManifest.xml` 中声明了 `SYSTEM_ALERT_WINDOW` 权限（由于已有的悬浮计算器功能），因此您可以直接使用该权限并在其他应用上层显示可拖动的圆圈。

2. **模拟连续点击 (Simulated Auto-Clicking)**
   * **技术方案**：使用 Android 的 `AccessibilityService`（无障碍服务）。自 Android 7.0 (API 24) 起，系统提供了 `dispatchGesture()` API，可以在无需 Root 的情况下在屏幕任意坐标模拟点击、滑动等手势。
   * **可行性**：可行。但需要注意：本项目 `minSdkVersion` 为 21，而模拟点击需要 API 24，这意味着连点功能仅在 Android 7.0 及以上设备生效。

3. **双键触发 (Volume+ & Volume- Trigger)**
   * **技术方案**：同样依赖于 `AccessibilityService`。在其 `onKeyEvent()` 回调中，可以拦截和监听全局的实体按键事件。当检测到音量加和音量减同时按下时，即可启动或停止模拟点击逻辑。
   * **可行性**：完全可行。

4. **视觉反馈 (Visual Feedback)**
   * **技术方案**：悬浮窗中的圆圈 View 可以通过属性动画（如 `ScaleAnimation` 或 `ViewPropertyAnimator`）在每次成功执行 `dispatchGesture` 时瞬间缩小再还原。
   * **可行性**：完全可行。

## 2. User Review Required (需要用户确认的事项)

> [!WARNING]
> **权限要求**：`AccessibilityService` (无障碍服务) **无法在代码中静默开启**。用户必须在手机的“设置 -> 无障碍 -> 已安装的服务”中手动找到本应用并开启，功能才能生效。这可能会增加用户使用该功能的步骤。

> [!IMPORTANT]
> **系统版本限制**：模拟点击功能 `dispatchGesture()` 最低要求 **Android 7.0 (API Level 24)**。在低于 7.0 的手机上此模块无法生效。

## 3. 详细实施步骤 (Implementation Plan)

### 3.1 核心组件 (`AutoClickerService`)
* 新增 `AutoClickerService` 类继承自 `AccessibilityService`。
* 在 `AndroidManifest.xml` 中注册该服务，并配置 `android.permission.BIND_ACCESSIBILITY_SERVICE` 及 `accessibility_service_config.xml`。
* **职责**：
  1. 监听全局按键事件 (`onKeyEvent`)，判断音量+和音量-是否同时按下。
  2. 管理悬浮窗的创建、显示、销毁和拖动逻辑。
  3. 维护一个 `Handler` 或 `Timer`，按照指定的时间间隔（如 100ms）在悬浮窗中心坐标执行点击手势，持续指定的时长（如 60s）。

### 3.2 用户界面与设置 (Settings UI)
在应用的设置页面 (Preference) 中增加一个新的模块【连点辅助】：
* **总开关**：开启/关闭连点辅助。
* **时间间隔 (ms)**：输入框，默认 100。
* **连点时长 (s)**：输入框，默认 60。

### 3.3 悬浮窗视图 (Overlay View)
* 创建一个简单的 XML 布局或代码生成的 View，内部是一个圆形的 Drawable（例如红色半透明圆圈带有中心点指示）。
* 为该 View 实现 `OnTouchListener` 以支持拖拽更改位置。
* 实现 `animateClick()` 方法：通过动画迅速将圆圈 `scale` 缩放至 `0.8` 并还原到 `1.0`。

## 4. 验证计划 (Verification Plan)

### 手动测试
1. 在设置中开启连点辅助。
2. 确保系统授权了“显示在其他应用上层”及“无障碍服务”。
3. 检查悬浮圆圈是否出现在屏幕上且可拖动。
4. 打开任意其他应用（如浏览器或计算器），同时按下音量+和音量-。
5. 验证是否开始以设定频率产生点击，且圆圈会执行缩放动画。
6. 验证到达指定时长后点击自动停止。

请确认此实现方案是否符合您的预期？如无问题，我将开始编写代码。
