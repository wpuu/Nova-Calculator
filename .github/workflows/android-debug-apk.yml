name: Android Debug APK

on:
  push:
    branches:
      - main
      - commercial/nova-ai-v1
      - 'reconcile/**'
  pull_request:
    branches:
      - main
      - commercial/nova-ai-v1
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-debug-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Node 22
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Adapt pinned plotter library for current Android Gradle Plugin
        run: |
          cat > plotter/lib/build.gradle <<'EOF'
          apply plugin: 'com.android.library'

          android {
              namespace 'org.solovyev.android.plotter'
              compileSdk 36

              defaultConfig {
                  minSdk 21
                  targetSdk 36
              }
          }

          dependencies {
              implementation fileTree(dir: 'libs', include: ['*.jar'])
              implementation 'androidx.annotation:annotation:1.8.2'
          }
          EOF

          python3 - <<'PY2'
          from pathlib import Path
          root = Path('plotter/lib/src/main/java')
          count = 0
          for path in root.rglob('*.java'):
              text = path.read_text(encoding='utf-8')
              updated = text.replace('android.support.annotation.', 'androidx.annotation.')
              if updated != text:
                  path.write_text(updated, encoding='utf-8')
                  count += 1
          print(f'Migrated AndroidX annotation imports in {count} Plotter Java files')
          PY2

      - name: Verify commercial source guard
        run: bash scripts/verify-commercial-source.sh

      - name: Verify commercial docs and licensing
        run: bash scripts/verify-commercial-docs.sh

      - name: Verify Nova Android identities
        run: bash scripts/verify-nova-identities.sh

      - name: Verify production release guard contract
        env:
          NOVA_AI_GATEWAY_URL: https://ci.nova-calculator.dev/api/ai
          NOVA_ANONYMOUS_SESSION_URL: https://ci.nova-calculator.dev/api/session
          NOVA_BILLING_URL: https://ci.nova-calculator.dev/api/billing
          NOVA_PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER: '123456789'
          NOVA_VERSION_CODE: '20001'
          NOVA_VERSION_NAME: 0.2.0-ci
        run: |
          if env -u NOVA_BILLING_URL bash scripts/verify-production-release-config.sh >/dev/null 2>&1; then
            echo 'Production release guard unexpectedly accepted missing billing URL' >&2
            exit 1
          fi
          bash scripts/verify-production-release-config.sh

      - name: Verify Accessibility disclosure and scope
        run: bash scripts/verify-accessibility-disclosure.sh

      - name: Test gateway capacity core
        run: cd gateway && npm test

      - name: Test commercial core contracts
        run: |
          ./gradlew :app:testDebugUnitTest \
            --tests 'org.solovyev.android.calculator.entitlement.*Test' \
            --tests 'org.solovyev.android.calculator.ai.*Test' \
            --tests 'org.solovyev.android.calculator.billing.*Test' \
            --tests 'org.solovyev.android.calculator.autoclicker.*Test' \
            --stacktrace --no-daemon

      - name: Create disposable CI upload key
        run: |
          mkdir -p "$RUNNER_TEMP/nova-ci-signing"
          keytool -genkeypair \
            -storetype JKS \
            -keystore "$RUNNER_TEMP/nova-ci-signing/nova-upload.jks" \
            -storepass nova-ci-store-pass \
            -alias nova-ci-upload \
            -keypass nova-ci-key-pass \
            -keyalg RSA \
            -keysize 2048 \
            -validity 3650 \
            -dname "CN=Nova CI, OU=CI, O=Nova, L=Test, S=Test, C=US"

      - name: Verify Play release configuration and build signed AAB
        env:
          NOVA_ANDROID_PACKAGE_NAME: com.wpuu.novacalculator
          NOVA_AI_GATEWAY_URL: https://ci.nova-calculator.dev/api/ai
          NOVA_ANONYMOUS_SESSION_URL: https://ci.nova-calculator.dev/api/session
          NOVA_BILLING_URL: https://ci.nova-calculator.dev/api/billing
          NOVA_PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER: '123456789'
          NOVA_VERSION_CODE: '20001'
          NOVA_VERSION_NAME: 0.2.0-ci
          NOVA_UPLOAD_KEYSTORE_PATH: ${{ runner.temp }}/nova-ci-signing/nova-upload.jks
          NOVA_UPLOAD_STORE_PASSWORD: nova-ci-store-pass
          NOVA_UPLOAD_KEY_ALIAS: nova-ci-upload
          NOVA_UPLOAD_KEY_PASSWORD: nova-ci-key-pass
        run: ./gradlew :app:verifyNovaPlayReleaseConfig :app:bundleRelease --stacktrace --no-daemon

      - name: Build debug APK
        run: ./gradlew :app:assembleDebug --stacktrace --no-daemon

      - name: Collect APK and CI release AAB
        if: success()
        run: |
          mkdir -p build-artifacts
          cp app/build/outputs/apk/debug/app-debug.apk build-artifacts/Nova-Calculator-commercial-debug.apk
          cp app/build/outputs/bundle/release/app-release.aab build-artifacts/Nova-Calculator-commercial-ci-release.aab
          sha256sum build-artifacts/Nova-Calculator-commercial-debug.apk build-artifacts/Nova-Calculator-commercial-ci-release.aab | tee build-artifacts/SHA256SUMS.txt

      - name: Upload build artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Nova-Calculator-commercial-builds
          path: build-artifacts/
          if-no-files-found: error
          retention-days: 14
